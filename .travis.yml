image: naonak/docker-apache-php7-laravel

before_script:
- apt-get update
# Install dependencies
- bash ci/docker_install.sh > /dev/null
- composer install

test:app:
  script:
  - echo "hello world!"
test:runUnitTest:
  script:
  - ./vendor/bin/phpunit tests/Unit/BoxTest.php
